apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: redis-data
  namespace: tradistonks
  labels:
    app: redis-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
status: {}
